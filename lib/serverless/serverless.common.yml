custom:
  secrets:
    apiSecret: ${ssm:/bekk-idrett/api/secret}
  region: eu-west-1
  bundle:
    tsConfig: "../../tsconfig.json"
    sourcemaps: false
    caching: false
  stage:
    prod: prod
    dev: dev
